# Atlas dependencies — exact versions, tested together on Python 3.11.15.
# Install with:
#     pip install -r requirements.txt
# Pinned for reproducibility so every build phase runs against the same APIs.

# --- Orchestration: the multi-agent graph ---
langgraph==1.2.6
langchain==1.3.11
langchain-core==1.4.8

# --- LLM providers (Gemini is the default; the others enable swapping) ---
langchain-google-genai==4.2.5
langchain-openai==1.3.3
langchain-anthropic==1.4.7

# --- Web search + page extraction ---
tavily-python==0.7.26

# --- Tracing / observability (Phase 4) ---
langfuse==4.10.0

# --- UI (Phase 6) ---
streamlit==1.58.0

# --- Config / secrets loading ---
python-dotenv==1.2.2
